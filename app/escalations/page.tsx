const EscalationPage = () => {
  return (
    <div>
      You are curious, I like you; but I dont need this endpoint, just the
      dynamic one tied to it, so go away
    </div>
  );
};
export default EscalationPage;
