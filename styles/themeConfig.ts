import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    fontFamily: "sans-serif",
    colorPrimary: "#52C41A",
    borderRadius: 8,
  },
};

export default theme;
